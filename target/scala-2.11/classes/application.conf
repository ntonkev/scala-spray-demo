akka {
  loggers          = [akka.event.slf4j.Slf4jLogger]
  loglevel         = debug
  log-dead-letters = off
  log-dead-letters-during-shutdown = off

  actor {
    debug {
      receive = on
      lifecycle = on
    }
  }
}

http {
  interface="localhost"
  port=8080
}
